# 更新日志 / Changelog

## 2024-12-18 - 实例导航模式与增强功能

### 🎯 核心新功能

#### 1. 实例导航模式 (Instance Navigation Mode)
- **快捷键**: `Ctrl+I` 切换模式
- **导航快捷键**: `A`/`D` 键在所有实例间跨图导航
- **功能特性**:
  - 在当前图像内按顺序切换实例
  - 当前图像实例遍历完毕后，自动跳转到下一张有标注的图像
  - 支持循环遍历（到最后一张后回到第一张）
  - 自动跳过没有标注的图像
  - 删除实例后自动选中下一个实例，保持导航连续性

#### 2. 视觉增强效果
- **粗边框**: 选中实例边框宽度为正常的2倍
- **无填充**: 选中实例不显示半透明填充色，便于查看边框内容
- **十字定位线**: 
  - 通过选中实例中心的水平/垂直虚线定位
  - 十字线颜色与实例边框颜色一致
  - 十字线不穿过边框内部，只显示在边框外侧

#### 3. 实例自动排序 (Auto Sort Instances)
- **功能**: 按实例中心位置自动排序
- **排序规则**: X坐标优先，Y坐标次之（从左到右，从上到下）
- **应用场景**: 配合实例导航模式，使导航顺序符合阅读习惯
- **触发时机**: 
  - 开启功能时立即对当前图像排序
  - 加载新图像时自动排序（如已开启）

#### 4. 标签编辑增强
- **Enter快捷键**: 实例导航模式下按Enter打开标签编辑对话框
- **键盘导航**: 
  - 对话框打开后自动聚焦到标签列表
  - 使用上下箭头键选择标签
  - 再次按Enter确认修改
- **Delete按钮**: 标签编辑对话框新增删除按钮，可直接删除当前实例

#### 5. Ctrl+双击删除
- **功能**: 启用后可通过Ctrl+双击快速删除实例
- **优先级**: 悬停实例 > 选中实例
- **开关**: View菜单中的"Ctrl+Double-Click Delete"选项

### 🔧 技术改进

#### 焦点管理优化
- 实例导航模式下自动将焦点设置到Canvas，确保键盘事件正确响应
- 切换实例后保持焦点在Canvas上
- 普通模式下保持原有焦点逻辑（标签列表优先）

#### Bug修复
- 修复`show_bounding_box_from_annotation_file`中file_path为None时的崩溃
- 修复实例导航模式下shape不在当前列表中导致的ValueError
- 修复标签列表重建时的属性缺失问题
- 修复Enter键事件判断顺序问题

### 📝 使用流程示例

**典型标注审核流程**:
1. 打开文件夹并设置保存目录
2. 启用"实例导航模式" (Ctrl+I)
3. 启用"自动排序实例"（可选，便于按位置顺序审核）
4. 使用A/D键在所有实例间快速导航
5. 按Enter键打开标签编辑
6. 使用上下键选择正确标签
7. 按Enter确认或点击Delete删除错误标注
8. 继续用D键导航到下一个实例

### 🎨 UI变更
- View菜单新增3个选项:
  - "Ctrl+Double-Click Delete" - Ctrl+双击删除开关
  - "Instance Navigation Mode" (Ctrl+I) - 实例导航模式开关  
  - "Auto Sort Instances" - 自动排序实例开关
- 标签编辑对话框新增"Delete 删除"按钮

### ⌨️ 新增快捷键
| 快捷键 | 功能 | 模式限制 |
|--------|------|----------|
| `Ctrl+I` | 切换实例导航模式 | 无 |
| `A` | 上一个实例（实例导航模式下） | 实例导航模式 |
| `D` | 下一个实例（实例导航模式下） | 实例导航模式 |
| `Enter` | 打开标签编辑（实例导航模式下） | 实例导航模式 |
| `Ctrl+双击` | 删除悬停/选中的实例 | 需启用功能 |

### 🔄 兼容性
- 所有新功能默认关闭，不影响原有工作流
- 保持与原版labelImg的文件格式完全兼容
- 支持PascalVOC、YOLO、CreateML格式

---

## 版本信息
- 基于: labelImg v1.8.x
- 修改日期: 2024-12-18
- Python版本: 3.x
- 依赖: PyQt5
